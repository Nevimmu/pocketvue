{
	"name": "todo",
	"version": "0.0.0",
	"scripts": {
		"release": "commit-and-tag-version"
	},
	"devDependencies": {
		"commit-and-tag-version": "^12.5.1"
	},
	"commit-and-tag-version": {
		"scripts": {
			"postbump": "cd frontend && npm install && cd .."
		}
	}
}
